# 安全策略

## 支持范围

SKIS 目前处于 `0.0.x` 预发布阶段，尚无生产支持版本。安全修复只进入当前 `main` 分支；正式版本发布后，本表会按
支持周期更新。

| 版本               | 安全更新 |
|------------------|------|
| `main` / 最新预发布版本 | 尽力支持 |
| 更早的预发布快照         | 不支持  |

## 私密报告漏洞

请不要创建公开 Issue，也不要在日志、测试或 Pull Request 中提交利用代码、凭据或真实敏感数据。请使用
GitHub 的
[私密安全公告](https://github.com/xhufang/skis/security/advisories/new) 报告，并提供：

- 受影响的版本、模块和配置；
- 可复现的最小步骤或测试；
- 影响范围及已知缓解措施；
- 如可行，提供不含真实秘密信息的修复建议。

维护者会尽快确认收到报告，在完成影响分析和修复前协调披露时间。若 GitHub 私密安全公告不可用，请先通过维护者的
GitHub 主页请求一个私密联系渠道，不要公开漏洞细节。

## 安全基线

SKIS 的安全设计要求包括参数化 SQL、标识符校验、默认拒绝无条件批量更新/删除、隐藏敏感绑定参数，以及发布时生成
SBOM 和依赖许可证清单。发现这些约束被绕过时，请按漏洞流程报告。
